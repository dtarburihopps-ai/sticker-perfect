# Локальные настройки Claude Code — у каждого свои, в репозиторий не нужны
.claude/settings.local.json

# Секреты: токен бота и всё в этом духе. В репозиторий не попадает никогда.
.env

# Мусор Windows и macOS
Thumbs.db
desktop.ini
.DS_Store

# Линейка для снятия координат — рабочий файл, пересоздаётся
tools/grid-preview.png

# Превью области дверцы — тоже рабочий файл, пересоздаётся door.py
tools/door-preview.png

# Превью коробки с карандашами — рабочий файл, пересоздаётся pencils.py
tools/pencils-preview.png

# Превью автомата — рабочий файл, пересоздаётся vending.py
tools/vending-preview.png

# Превью ящика с пуговицами — рабочие файлы, пересоздаются buttons.py
tools/buttons-preview.png
tools/buttons-preview-colour.png
