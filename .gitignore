# Локальные настройки Claude Code — у каждого свои, в репозиторий не нужны
.claude/settings.local.json

# Мусор Windows и macOS
Thumbs.db
desktop.ini
.DS_Store

# Собранная игра одним файлом. Получается из исходников командой
#   python build.py
# В репозитории не нужна: весит под двести килобайт и меняется целиком
# при каждой правке, раздувая историю.
sticker-perfect.html

# Линейка для снятия координат — рабочий файл, пересоздаётся
tools/grid-preview.png
